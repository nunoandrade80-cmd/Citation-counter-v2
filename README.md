# Citation-counter-v2
A Python script to fetch Europe PMC citation counts for PubMed IDs (PMIDs). Supports flexible input via a manual string or uploaded text file (.txt), handling both one-per-line or comma-separated PMID formats.
